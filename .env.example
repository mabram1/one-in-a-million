# Copy to .env.local and fill in for LOCAL cloud features (live multiplayer / auth).
# The anon key is PUBLIC by design (RLS-protected) — it is NOT the service_role key.
# In CI/deploy, set these as GitHub Actions repo VARIABLES (not secrets).
VITE_SUPABASE_URL=https://tsddumsxoclcjguczezr.supabase.co
VITE_SUPABASE_ANON_KEY=
